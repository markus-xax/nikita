# Интеграция фронтенда и бэкенда

## 📋 Обзор

Этот документ объясняет, как фронтенд (React + TypeScript) взаимодействует с бэкендом (Node.js + Express + PostgreSQL) через REST API. В качестве примера используется Todo-лист, который сохраняет данные в базе данных.

---

## 🏗️ Архитектура

```
┌─────────────────┐         HTTP/REST API         ┌─────────────────┐
│                 │ ◄─────────────────────────────► │                 │
│   React App     │                                 │  Express Server │
│   (Frontend)    │                                 │   (Backend)     │
│                 │                                 │                 │
│  - UI компоненты│                                 │  - REST routes  │
│  - API клиент   │                                 │  - Controllers  │
│  - State mgmt   │                                 │  - Prisma ORM   │
└─────────────────┘                                 └─────────────────┘
                                                           │
                                                           ▼
                                                  ┌─────────────────┐
                                                  │   PostgreSQL    │
                                                  │   (Database)    │
                                                  └─────────────────┘
```

---

## 🔧 Компоненты интеграции

### 1. **Бэкенд (Backend)**

#### CORS Middleware
```javascript
// backend/src/server.js

// Разрешаем запросы с фронтенда
app.use((req, res, next) => {
  res.header('Access-Control-Allow-Origin', 'http://localhost:5173'); // Vite dev server
  res.header('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS');
  res.header('Access-Control-Allow-Headers', 'Origin, X-Requested-With, Content-Type, Accept');
  
  // Обработка preflight запросов
  if (req.method === 'OPTIONS') {
    return res.sendStatus(200);
  }
  
  next();
});
```

**Что это делает:**
- Разрешает запросы с фронтенда (CORS - Cross-Origin Resource Sharing)
- Браузер блокирует запросы между разными доменами/портами по умолчанию
- CORS headers говорят браузеру: "этот сервер разрешает запросы с localhost:5173"

#### REST API Endpoints
```javascript
// backend/src/routes/todos.js

GET    /todos        // Получить все задачи
POST   /todos        // Создать новую задачу
GET    /todos/:id    // Получить задачу по ID
PUT    /todos/:id    // Обновить задачу
DELETE /todos/:id    // Удалить задачу
```

**Пример запроса:**
```javascript
// POST /todos
{
  "text": "Изучить React"
}

// Ответ: 201 Created
{
  "id": 1,
  "text": "Изучить React",
  "completed": false
}
```

---

### 2. **Фронтенд (Frontend)**

#### API Клиент
```typescript
// src/lib/api.ts

const API_BASE_URL = 'http://localhost:4000';

export async function getAllTodos(): Promise<Todo[]> {
  const response = await fetch(`${API_BASE_URL}/todos`);
  return handleResponse<Todo[]>(response);
}

export async function createTodo(data: CreateTodoDto): Promise<Todo> {
  const response = await fetch(`${API_BASE_URL}/todos`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(data),
  });
  return handleResponse<Todo>(response);
}
```

**Что происходит:**
1. `fetch()` отправляет HTTP запрос на бэкенд
2. Бэкенд обрабатывает запрос и возвращает JSON
3. Функция парсит ответ и возвращает типизированные данные

---

### 3. **React Компонент**

#### Загрузка данных при монтировании
```typescript
// src/pages/examples/InteractivePage.tsx

useEffect(() => {
  loadTodos();
}, []);

const loadTodos = async () => {
  try {
    setLoading(true);
    const data = await getAllTodos(); // Вызов API
    setTodos(data); // Обновление состояния
  } catch (err) {
    setError(err.message);
  } finally {
    setLoading(false);
  }
};
```

**Как это работает:**
1. `useEffect` запускается один раз при монтировании компонента
2. Вызывается `getAllTodos()` - отправляется GET запрос на `/todos`
3. Бэкенд возвращает массив задач из PostgreSQL
4. React обновляет UI автоматически при изменении `todos`

#### Создание новой задачи
```typescript
const addTodo = async () => {
  const text = todoInput.trim();
  if (!text) return;

  try {
    // Отправляем POST запрос на бэкенд
    const newTodo = await createTodo({ text });
    
    // Обновляем локальное состояние
    setTodos((prev) => [...prev, newTodo]);
    setTodoInput("");
  } catch (err) {
    setError(err.message);
  }
};
```

**Поток данных:**
1. Пользователь вводит текст и нажимает "Добавить"
2. `createTodo()` отправляет POST `/todos` с `{ text: "..." }`
3. Бэкенд создает запись в PostgreSQL через Prisma
4. Бэкенд возвращает созданную задачу с ID
5. React добавляет новую задачу в состояние
6. UI автоматически обновляется

#### Обновление задачи
```typescript
const toggleTodo = async (id: number) => {
  const todo = todos.find((t) => t.id === id);
  
  // Отправляем PUT запрос
  const updatedTodo = await updateTodo(id, {
    completed: !todo.completed,
  });
  
  // Обновляем локальное состояние
  setTodos((prev) =>
    prev.map((t) => (t.id === id ? updatedTodo : t))
  );
};
```

**Что происходит:**
1. Пользователь кликает на задачу
2. Отправляется PUT `/todos/:id` с новым значением `completed`
3. Бэкенд обновляет запись в PostgreSQL
4. Бэкенд возвращает обновленную задачу
5. React обновляет состояние и UI

---

## 🔄 Полный цикл запроса

### Пример: Создание задачи

```
1. Пользователь вводит "Изучить React" и нажимает "Добавить"
   │
   ▼
2. React вызывает addTodo()
   │
   ▼
3. API клиент отправляет fetch('POST /todos', { text: "Изучить React" })
   │
   ▼
4. Браузер отправляет HTTP запрос на http://localhost:4000/todos
   │
   ▼
5. Express получает запрос → routes/todos.js → validateCreateTodo middleware
   │
   ▼
6. Валидация проходит → todosController.createTodo()
   │
   ▼
7. Prisma создает запись в PostgreSQL: INSERT INTO todos (text, completed) VALUES (...)
   │
   ▼
8. Prisma возвращает созданную запись с ID
   │
   ▼
9. Controller возвращает JSON: { id: 1, text: "Изучить React", completed: false }
   │
   ▼
10. Express отправляет ответ с кодом 201 Created
    │
    ▼
11. Браузер получает ответ → fetch() возвращает Promise
    │
    ▼
12. API клиент парсит JSON и возвращает Todo объект
    │
    ▼
13. React обновляет состояние: setTodos([...prev, newTodo])
    │
    ▼
14. React автоматически перерисовывает компонент
    │
    ▼
15. Пользователь видит новую задачу в списке! ✅
```

---

## 📡 HTTP методы и их использование

| Метод | Endpoint | Что делает | Когда использовать |
|-------|----------|------------|-------------------|
| **GET** | `/todos` | Получить все задачи | При загрузке страницы |
| **GET** | `/todos/:id` | Получить одну задачу | Для детального просмотра |
| **POST** | `/todos` | Создать задачу | При добавлении новой задачи |
| **PUT** | `/todos/:id` | Обновить задачу | При изменении текста или статуса |
| **DELETE** | `/todos/:id` | Удалить задачу | При нажатии "Удалить" |

---

## 🛡️ Обработка ошибок

### На фронтенде
```typescript
try {
  const todos = await getAllTodos();
  setTodos(todos);
} catch (err) {
  // Ошибка сети, сервера или валидации
  setError(err instanceof Error ? err.message : "Ошибка загрузки");
}
```

### На бэкенде
```javascript
// middleware/validation.js - валидация данных
if (!text || text.trim().length === 0) {
  return res.status(400).json({
    error: 'Validation Error',
    message: 'Текст задачи обязателен'
  });
}

// controllers/todosController.js - обработка ошибок БД
try {
  const todo = await prisma.todo.create({ data: { text } });
  res.status(201).json(todo);
} catch (error) {
  res.status(500).json({
    error: 'Database Error',
    message: 'Не удалось создать задачу'
  });
}
```

---

## 🎯 Ключевые концепции

### 1. **Асинхронность**
- Все операции с API асинхронные (`async/await`)
- UI не блокируется во время запросов
- Используем `loading` состояние для показа индикатора

### 2. **Состояние (State)**
- React хранит данные в `useState`
- При изменении состояния React автоматически обновляет UI
- Состояние синхронизируется с данными из БД

### 3. **Оптимистичные обновления**
```typescript
// Можно обновить UI сразу, не дожидаясь ответа сервера
setTodos((prev) => [...prev, { id: Date.now(), text, completed: false }]);

// А потом синхронизировать с сервером
const savedTodo = await createTodo({ text });
```

### 4. **Обработка загрузки**
```typescript
const [loading, setLoading] = useState(false);

// Показываем индикатор загрузки
{loading && <div>Загрузка...</div>}

// Блокируем кнопки во время запроса
<Button disabled={loading}>Добавить</Button>
```

---

## 🚀 Как запустить интеграцию

### 1. Запустить бэкенд
```bash
cd backend
npm install
npm run db:migrate  # Создать таблицы в PostgreSQL
npm start           # Запустить сервер на http://localhost:4000
```

### 2. Запустить фронтенд
```bash
npm install
npm run dev         # Запустить на http://localhost:5173
```

### 3. Проверить интеграцию
1. Откройте http://localhost:5173/examples/interactive
2. Добавьте задачу - она сохранится в PostgreSQL
3. Обновите страницу - задачи загрузятся с сервера
4. Откройте другую вкладку - изменения синхронизируются

---

## 🔍 Отладка

### Проверка запросов в браузере
1. Откройте DevTools (F12)
2. Вкладка **Network**
3. Фильтр: **Fetch/XHR**
4. Вы увидите все HTTP запросы к бэкенду

### Проверка ответов сервера
```bash
# Логи бэкенда показывают все запросы
cd backend
npm start

# Вы увидите:
# 💾 Запрос к PostgreSQL через Prisma
# 📦 Данные получены из Redis кеша
```

### Проверка базы данных
```bash
cd backend
npm run db:studio  # Откроет Prisma Studio для просмотра данных
```

---

## 📚 Дополнительные ресурсы

- [REST API Best Practices](https://restfulapi.net/)
- [Fetch API Documentation](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)
- [React useEffect Hook](https://react.dev/reference/react/useEffect)
- [CORS Explained](https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS)
- [Prisma Documentation](https://www.prisma.io/docs)

---

## 🎓 Что изучить дальше

1. **Оптимизация запросов**
   - Кеширование на клиенте (React Query, SWR)
   - Debounce для поиска
   - Pagination для больших списков

2. **Безопасность**
   - Аутентификация (JWT токены)
   - Авторизация (проверка прав доступа)
   - Валидация на клиенте и сервере

3. **Улучшение UX**
   - Оптимистичные обновления
   - Офлайн режим (Service Workers)
   - WebSocket для real-time обновлений

4. **Тестирование**
   - Unit тесты для API клиента
   - Integration тесты для API endpoints
   - E2E тесты для полного цикла

---

## ✅ Резюме

**Интеграция работает так:**

1. **Фронтенд** отправляет HTTP запросы через `fetch()`
2. **Бэкенд** обрабатывает запросы через Express routes
3. **База данных** хранит данные через Prisma ORM
4. **React** автоматически обновляет UI при изменении состояния

**Ключевые моменты:**
- ✅ CORS настроен для разрешения запросов между портами
- ✅ API клиент абстрагирует работу с HTTP
- ✅ React состояние синхронизируется с данными из БД
- ✅ Ошибки обрабатываются и показываются пользователю
- ✅ Loading состояния улучшают UX

Теперь фронтенд и бэкенд работают вместе! 🎉
