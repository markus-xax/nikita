# 🚀 Быстрый старт: Интеграция фронтенда и бэкенда

## Шаг 1: Запустить бэкенд

```bash
cd backend
npm install
npm run db:migrate  # Создать таблицы в PostgreSQL
npm start           # Сервер запустится на http://localhost:4000
```

**Проверка:** Откройте http://localhost:4000/todos - должен вернуться пустой массив `[]`

## Шаг 2: Запустить фронтенд

В новом терминале:

```bash
npm install
npm run dev         # Фронтенд запустится на http://localhost:5173
```

## Шаг 3: Протестировать интеграцию

1. Откройте http://localhost:5173/examples/interactive
2. Прокрутите до секции "Todo-лист (с бэкендом)"
3. Добавьте задачу - она сохранится в PostgreSQL
4. Обновите страницу - задачи загрузятся с сервера
5. Откройте Prisma Studio для просмотра данных:
   ```bash
   cd backend
   npm run db:studio
   ```

## ✅ Что должно работать

- ✅ Добавление задач → сохраняется в БД
- ✅ Переключение статуса → обновляется в БД
- ✅ Удаление задач → удаляется из БД
- ✅ Загрузка при открытии страницы → данные из БД

## 🔍 Отладка

Если что-то не работает:

1. **Проверьте, что бэкенд запущен:**
   ```bash
   curl http://localhost:4000/todos
   ```

2. **Проверьте CORS в DevTools:**
   - Откройте DevTools (F12) → Network
   - Если видите ошибку CORS, проверьте настройки в `backend/src/server.js`

3. **Проверьте логи бэкенда:**
   - В консоли бэкенда должны быть сообщения о запросах

4. **Проверьте базу данных:**
   ```bash
   cd backend
   npm run db:studio
   ```

## 📚 Подробная документация

См. [FRONTEND_BACKEND_INTEGRATION.md](./FRONTEND_BACKEND_INTEGRATION.md) для полного объяснения как работает интеграция.
