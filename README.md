# Frontend Playground · React 18 + Tailwind CSS

Коллекция классических «ванильных» примеров (HTML, CSS, JS), портированная на современный стек: React 18, Vite, Tailwind CSS и Radix UI. Проект показывает, как привычные упражнения вроде форм, счетчиков и манипуляций с DOM выглядят в декларативном мире компонентов.

---

## ✨ Что внутри
- **React 18 + Vite** — быстрая сборка, горячая перезагрузка и современный DX.
- **Tailwind CSS** — утилитарные классы и дизайн‑токены через CSS‑переменные.
- **shadcn/ui + Radix UI** — переиспользуемые компоненты (кнопки, карточки, диалоги).
- **React Router** — маршруты для каждой сцены (`/examples/*`, `roadmap`, `resources`).
- **Темизация** — глобальный `ThemeProvider`, переключение светлой/темной темы и локальные демонстрации.
- **Zod** — схема и валидация управляемой формы.

---

## 🚀 Быстрый старт
```bash
npm install
npm run dev
```

Откройте `http://localhost:5173` — Vite автоматически откроет браузер. Для билда и предпросмотра:

```bash
npm run build
npm run preview
```

---

## 📁 Структура
```
src/
  App.tsx                # Маршруты и раскладка
  components/            # UI-компоненты и провайдеры темы
  data/examples.ts       # Карточки с описанием сцен
  pages/                 # Страницы и примеры (basic HTML, forms, DOM и т.д.)
  styles/globals.css     # Tailwind слоя base/components/utilities + CSS-переменные
```

- `components/ui/*` — обертки shadcn/ui поверх Radix primitives (button, badge, card, dialog).
- `components/theme-provider.tsx` — переключение классов `light`/`dark`, хранение выбора в `localStorage`.
- `pages/examples/*` — React-аналог каждого ванильного примера:
  - `BasicHtmlPage` — семантическая разметка и таблицы на JSX.
  - `FormsPage` — управляемая форма с валидацией и пресетами.
  - `InteractivePage` — счетчик, todo, темы и модальное окно.
  - `LayoutsPage` — playground для Flexbox и Grid.
  - `DomPage` — CRUD по состояниям вместо ручного DOM API.

---

## 🎨 Tailwind & дизайн‑токены
- `src/styles/globals.css` подключает `@tailwind base`, `components`, `utilities`, объявляет CSS‑переменные (`--background`, `--primary`, `--radius`) и распространяет их через `@apply`.
- `tailwind.config.ts` использует темный режим по классу (`darkMode: ["class"]`), читает токены из `:root`, подключает `tailwindcss-animate` и кастомные ключевые кадры.
- Дополнительные утилиты: `class-variance-authority` + `tailwind-merge` для вариативных компонентов (`button`, `badge`).

---

## 🛠 Скрипты npm
- `npm run dev` — запуск Vite dev-server.
- `npm run build` — production-сборка.
- `npm run preview` — предпросмотр собранного приложения.
- `npm run lint` — проверка (правила ESLint настроены на TypeScript + React Hooks).

---

## 📚 Полезные ссылки
- [React 18 Docs](https://react.dev/)
- [Tailwind CSS](https://tailwindcss.com/)
- [shadcn/ui](https://ui.shadcn.com/)
- [Radix UI](https://www.radix-ui.com/)
- [Zod](https://zod.dev/)

## 🔄 Ванильный JS vs React

Подробное сравнение подходов и объяснение различий между ванильным JavaScript и React доступно в документе **[VANILLA_VS_REACT.md](./VANILLA_VS_REACT.md)**.

В документе описаны:
- Концептуальные различия (императивный vs декларативный подход)
- Управление состоянием и DOM
- Обработка событий и форм
- Компонентный подход
- Практические примеры с кодом
- Когда использовать каждый подход

## 🔗 Интеграция с бэкендом

Пример интеграции фронтенда с бэкендом через REST API описан в документе **[FRONTEND_BACKEND_INTEGRATION.md](./FRONTEND_BACKEND_INTEGRATION.md)**.

В документе объясняется:
- Как работает взаимодействие между React и Express
- Архитектура клиент-серверного приложения
- CORS и настройка запросов между портами
- Полный цикл HTTP запроса от UI до базы данных
- Обработка ошибок и loading состояний
- Практический пример с Todo-листом, сохраняющим данные в PostgreSQL

**Пример:** Todo-лист на странице `/examples/interactive` теперь работает с реальным бэкендом и сохраняет данные в PostgreSQL!

---

## 🗂️ Источник ванильных примеров
Исходные HTML/CSS/JS файлы сохранены в каталоге `examples/`. Их можно использовать для сравнения подходов или как отправную точку для собственных миграций.

---

## 🤝 Как использовать проект
- Показывать студентам, как одно и то же упражнение реализуется в ванильном JS и в React.
- Быстро прототипировать UI с Tailwind и shadcn/ui.
- Создавать собственные сцены: добавьте файл в `src/pages/examples`, пропишите маршрут в `App.tsx`, а описание — в `src/data/examples.ts`.

Удачи в изучении и адаптации фронтенд-паттернов к современному стеку!

