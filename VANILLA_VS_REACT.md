# Ванильный JavaScript vs React: Сравнение подходов

## 📚 Введение

Этот документ объясняет ключевые различия между разработкой на ванильном JavaScript и React. Проект содержит примеры обоих подходов, чтобы показать, как одна и та же задача решается по-разному.

---

## 🎯 Основные концептуальные различия

### 1. **Императивный vs Декларативный подход**

#### Ванильный JavaScript (Императивный)
```javascript
// Вы говорите КАК что-то сделать
const counter = document.getElementById('counterValue');
const button = document.getElementById('increaseBtn');

button.addEventListener('click', function() {
    const currentValue = parseInt(counter.textContent);
    counter.textContent = currentValue + 1;
    counter.style.transform = 'scale(1.2)';
    setTimeout(() => {
        counter.style.transform = 'scale(1)';
    }, 200);
});
```

**Особенности:**
- Вы вручную находите элементы DOM (`getElementById`, `querySelector`)
- Вы вручную изменяете их свойства (`textContent`, `style`)
- Вы вручную управляете событиями (`addEventListener`)
- Код описывает **шаги** выполнения

#### React (Декларативный)
```tsx
// Вы говорите ЧТО должно быть отображено
const [counter, setCounter] = useState(0);

<button onClick={() => setCounter(counter + 1)}>
    Увеличить
</button>
<div className={counter > 0 ? 'highlight' : ''}>
    {counter}
</div>
```

**Особенности:**
- Вы описываете **желаемое состояние** UI
- React сам решает, как обновить DOM
- Изменения происходят через обновление состояния (`useState`)
- Код описывает **результат**, а не процесс

---

### 2. **Управление состоянием**

#### Ванильный JavaScript
```javascript
// Состояние хранится в переменных
let todos = [];
let counter = 0;

// При изменении нужно вручную обновить DOM
function addTodo(text) {
    todos.push({ id: Date.now(), text, completed: false });
    renderTodos(); // Вызываем функцию обновления вручную
}

function renderTodos() {
    const list = document.getElementById('todoList');
    list.innerHTML = ''; // Очищаем весь список
    
    todos.forEach(todo => {
        const li = document.createElement('li');
        li.innerHTML = `<span>${todo.text}</span>`;
        list.appendChild(li);
    });
}
```

**Проблемы:**
- Нужно помнить вызывать `renderTodos()` после каждого изменения
- Легко забыть обновить UI
- Состояние и DOM могут рассинхронизироваться
- Приходится вручную очищать и пересоздавать элементы

#### React
```tsx
// Состояние управляется через хуки
const [todos, setTodos] = useState<TodoItem[]>([]);

function addTodo(text: string) {
    setTodos(prev => [...prev, { 
        id: Date.now(), 
        text, 
        completed: false 
    }]);
    // React автоматически перерисовывает компонент!
}

// UI автоматически обновляется при изменении состояния
return (
    <ul>
        {todos.map(todo => (
            <li key={todo.id}>{todo.text}</li>
        ))}
    </ul>
);
```

**Преимущества:**
- React автоматически отслеживает изменения состояния
- UI всегда синхронизирован с данными
- Не нужно вручную обновлять DOM
- React эффективно обновляет только изменённые части

---

### 3. **Работа с DOM**

#### Ванильный JavaScript
```javascript
// Создание элементов
const newItem = document.createElement('div');
newItem.className = 'card';
newItem.innerHTML = '<h3>Заголовок</h3>';
container.appendChild(newItem);

// Изменение стилей
const box = document.getElementById('styleBox');
box.style.color = '#ff0000';
box.style.fontSize = '24px';
box.classList.add('highlight');

// Удаление элементов
const item = document.getElementById('item-1');
item.parentNode.removeChild(item);
```

**Особенности:**
- Прямое манипулирование DOM
- Нужно вручную создавать, изменять и удалять элементы
- Легко допустить ошибки (например, забыть удалить обработчики событий)
- Сложно отслеживать, какие элементы были изменены

#### React
```tsx
// Просто описываете JSX
const [items, setItems] = useState<string[]>([]);

function addItem(text: string) {
    setItems(prev => [...prev, text]);
}

function removeItem(index: number) {
    setItems(prev => prev.filter((_, i) => i !== index));
}

// React сам управляет DOM
return (
    <div>
        {items.map((item, index) => (
            <div key={index} className="card">
                <h3>{item}</h3>
                <button onClick={() => removeItem(index)}>
                    Удалить
                </button>
            </div>
        ))}
    </div>
);
```

**Преимущества:**
- Не нужно вручную работать с DOM API
- React эффективно обновляет только изменённые элементы (Virtual DOM)
- Автоматическая очистка обработчиков событий
- Меньше багов, связанных с утечками памяти

---

### 4. **Обработка событий**

#### Ванильный JavaScript
```javascript
// Нужно вручную привязывать обработчики
const button = document.getElementById('addBtn');
button.addEventListener('click', handleClick);

// При удалении элемента нужно не забыть удалить обработчик
function removeTodo(id) {
    todos = todos.filter(todo => todo.id !== id);
    renderTodos(); // Но старые обработчики могут остаться!
}
```

**Проблемы:**
- Нужно вручную управлять жизненным циклом обработчиков
- Легко создать утечки памяти
- При динамическом создании элементов нужно вручную добавлять обработчики

#### React
```tsx
// Обработчики объявляются прямо в JSX
<button onClick={() => handleAdd()}>
    Добавить
</button>

// React автоматически управляет жизненным циклом
function TodoItem({ todo, onDelete }) {
    return (
        <div>
            <span>{todo.text}</span>
            <button onClick={() => onDelete(todo.id)}>
                Удалить
            </button>
        </div>
    );
}
```

**Преимущества:**
- Обработчики автоматически очищаются при размонтировании компонента
- Нет утечек памяти
- Более читаемый код (обработчики рядом с элементами)

---

### 5. **Формы и валидация**

#### Ванильный JavaScript
```javascript
const form = document.getElementById('registrationForm');

form.addEventListener('submit', function(e) {
    e.preventDefault();
    
    const name = document.getElementById('name').value;
    const email = document.getElementById('email').value;
    
    // Валидация вручную
    if (name.length < 2) {
        alert('Имя слишком короткое');
        return;
    }
    
    if (!email.includes('@')) {
        alert('Некорректный email');
        return;
    }
    
    // Отправка данных
    console.log({ name, email });
});
```

**Проблемы:**
- Нужно вручную читать значения из полей
- Валидация разбросана по коду
- Сложно синхронизировать состояние формы с UI
- Нужно вручную показывать/скрывать ошибки

#### React
```tsx
// Управляемые компоненты
const [formData, setFormData] = useState({
    name: '',
    email: ''
});
const [errors, setErrors] = useState({});

// Валидация с Zod
const schema = z.object({
    name: z.string().min(2, 'Имя слишком короткое'),
    email: z.string().email('Некорректный email')
});

function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    const result = schema.safeParse(formData);
    
    if (!result.success) {
        setErrors(result.error.formErrors.fieldErrors);
        return;
    }
    
    console.log(result.data);
}

// Поля автоматически синхронизированы
<input 
    value={formData.name}
    onChange={(e) => setFormData({...formData, name: e.target.value})}
/>
{errors.name && <span>{errors.name}</span>}
```

**Преимущества:**
- Состояние формы централизовано
- Валидация через схемы (Zod, Yup)
- Ошибки автоматически отображаются
- Легко сбросить форму или заполнить пресетами

---

### 6. **Компонентный подход**

#### Ванильный JavaScript
```javascript
// Функции для создания элементов
function createCard(title, description) {
    const card = document.createElement('div');
    card.className = 'card';
    card.innerHTML = `
        <h3>${title}</h3>
        <p>${description}</p>
    `;
    return card;
}

// Использование
const card1 = createCard('Заголовок 1', 'Описание 1');
container.appendChild(card1);
```

**Ограничения:**
- Нет переиспользования логики
- Сложно передавать данные между компонентами
- Нет стандартизированного способа создания компонентов

#### React
```tsx
// Переиспользуемый компонент
interface CardProps {
    title: string;
    description: string;
    onAction?: () => void;
}

function Card({ title, description, onAction }: CardProps) {
    return (
        <div className="card">
            <h3>{title}</h3>
            <p>{description}</p>
            {onAction && <button onClick={onAction}>Действие</button>}
        </div>
    );
}

// Использование
<Card 
    title="Заголовок 1" 
    description="Описание 1"
    onAction={() => console.log('Clicked!')}
/>
```

**Преимущества:**
- Легко переиспользовать компоненты
- Типизация через TypeScript
- Композиция компонентов
- Изоляция логики и стилей

---

## 📊 Сравнительная таблица

| Аспект | Ванильный JS | React |
|--------|-------------|-------|
| **Подход** | Императивный | Декларативный |
| **Управление DOM** | Вручную | Автоматически |
| **Состояние** | Переменные | Хуки (`useState`, `useReducer`) |
| **Обновление UI** | Вручную вызывать функции | Автоматически при изменении состояния |
| **События** | `addEventListener` | `onClick`, `onChange` в JSX |
| **Компоненты** | Функции создания элементов | Переиспользуемые компоненты |
| **Типизация** | Нет (или JSDoc) | TypeScript из коробки |
| **Производительность** | Зависит от реализации | Оптимизация через Virtual DOM |
| **Кривая обучения** | Низкая | Средняя |
| **Размер бандла** | Минимальный | ~40KB (React) |
| **Экосистема** | Огромная | Огромная + React-специфичная |

---

## 🎓 Когда использовать что?

### Ванильный JavaScript подходит для:
- ✅ Простые статические сайты
- ✅ Небольшие интерактивные элементы
- ✅ Когда критичен размер бандла
- ✅ Обучение основам веб-разработки
- ✅ Прототипирование без зависимостей

### React подходит для:
- ✅ Сложные интерактивные приложения
- ✅ Приложения с большим количеством состояния
- ✅ Командная разработка (компонентный подход)
- ✅ Когда нужна типизация (TypeScript)
- ✅ Большие проекты с переиспользованием компонентов
- ✅ SPA (Single Page Applications)

---

## 🔄 Пример: Счетчик

### Ванильный JavaScript
```javascript
let counter = 0;
const display = document.getElementById('counter');
const incBtn = document.getElementById('inc');
const decBtn = document.getElementById('dec');

function updateDisplay() {
    display.textContent = counter;
}

incBtn.addEventListener('click', () => {
    counter++;
    updateDisplay();
});

decBtn.addEventListener('click', () => {
    counter--;
    updateDisplay();
});
```

### React
```tsx
function Counter() {
    const [counter, setCounter] = useState(0);
    
    return (
        <div>
            <div>{counter}</div>
            <button onClick={() => setCounter(c => c + 1)}>+</button>
            <button onClick={() => setCounter(c => c - 1)}>-</button>
        </div>
    );
}
```

**Разница:**
- В ванильном JS: 15+ строк, нужно вручную обновлять DOM
- В React: 8 строк, автоматическое обновление

---

## 🔄 Пример: Список задач (TODO)

### Ванильный JavaScript
```javascript
let todos = [];

function addTodo(text) {
    todos.push({ id: Date.now(), text, completed: false });
    renderTodos();
}

function toggleTodo(id) {
    todos = todos.map(todo => 
        todo.id === id ? { ...todo, completed: !todo.completed } : todo
    );
    renderTodos();
}

function renderTodos() {
    const list = document.getElementById('todoList');
    list.innerHTML = '';
    
    todos.forEach(todo => {
        const li = document.createElement('li');
        li.className = todo.completed ? 'completed' : '';
        li.innerHTML = `
            <span onclick="toggleTodo(${todo.id})">${todo.text}</span>
            <button onclick="deleteTodo(${todo.id})">Удалить</button>
        `;
        list.appendChild(li);
    });
}
```

### React
```tsx
function TodoList() {
    const [todos, setTodos] = useState<Todo[]>([]);
    
    function addTodo(text: string) {
        setTodos(prev => [...prev, { 
            id: Date.now(), 
            text, 
            completed: false 
        }]);
    }
    
    function toggleTodo(id: number) {
        setTodos(prev => prev.map(todo => 
            todo.id === id ? { ...todo, completed: !todo.completed } : todo
        ));
    }
    
    return (
        <ul>
            {todos.map(todo => (
                <li key={todo.id} className={todo.completed ? 'completed' : ''}>
                    <span onClick={() => toggleTodo(todo.id)}>
                        {todo.text}
                    </span>
                    <button onClick={() => deleteTodo(todo.id)}>
                        Удалить
                    </button>
                </li>
            ))}
        </ul>
    );
}
```

**Разница:**
- В ванильном JS: нужно вручную вызывать `renderTodos()` после каждого изменения
- В React: UI обновляется автоматически при изменении `todos`

---

## 🚀 Преимущества React

1. **Декларативность**: Описываете ЧТО должно быть, а не КАК это сделать
2. **Компонентность**: Переиспользуемые блоки кода
3. **Автоматическое обновление**: UI синхронизирован с данными
4. **Экосистема**: Огромное количество библиотек и инструментов
5. **TypeScript**: Отличная поддержка типизации
6. **Производительность**: Virtual DOM оптимизирует обновления
7. **Разработка**: Hot reload, DevTools, отладка

---

## ⚠️ Недостатки React

1. **Размер**: React добавляет ~40KB к бандлу
2. **Кривая обучения**: Нужно изучить концепции (хуки, компоненты, JSX)
3. **Абстракция**: Скрывает детали работы с DOM
4. **Зависимости**: Нужны инструменты сборки (Vite, Webpack)

---

## 📝 Заключение

**Ванильный JavaScript** — отличный выбор для изучения основ и простых проектов. Он даёт полный контроль над DOM и не требует инструментов сборки.

**React** — мощный инструмент для создания сложных приложений. Он упрощает управление состоянием, компонентами и автоматизирует обновления UI.

**Рекомендация**: Начните с ванильного JavaScript, чтобы понять основы. Затем изучите React для создания современных приложений. Оба подхода важны и дополняют друг друга!

---

## 📚 Дополнительные ресурсы

- [React Documentation](https://react.dev/)
- [MDN Web Docs - JavaScript](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
