# 🏠 Домашние задания по Express

## Задача 1: Добавь новые маршруты (Базовый уровень)

**Цель:** Научиться создавать новые маршруты в Express.

**Задание:**
1. Создай маршрут `GET /time`, который возвращает текущее время в формате:
   ```json
   {
     "currentTime": "2024-01-15T10:30:00.000Z",
     "timezone": "UTC"
   }
   ```
2. Создай маршрут `GET /greet/:name`, который принимает имя в URL и возвращает приветствие:
   ```json
   {
     "message": "Привет, Иван!"
   }
   ```
   Пример: `GET /greet/Иван` → `{"message": "Привет, Иван!"}`

**Подсказки:**
- Используй `new Date()` для получения текущего времени
- В Express параметры маршрута доступны через `req.params`
- Используй `res.json()` для отправки ответа

---

## Задача 2: Создай простой CRUD для списка задач (Средний уровень)

**Цель:** Научиться работать с данными и разными HTTP методами.

**Задание:**
Создай in-memory хранилище (просто массив в памяти) для списка задач (todo) и реализуй CRUD операции:

1. **GET /todos** — получить все задачи
   ```json
   [
     { "id": 1, "text": "Купить молоко", "completed": false },
     { "id": 2, "text": "Сделать домашнее задание", "completed": true }
   ]
   ```

2. **POST /todos** — создать новую задачу
   - Тело запроса: `{ "text": "Новая задача" }`
   - Ответ: созданная задача с автоматически присвоенным `id`

3. **GET /todos/:id** — получить задачу по ID
   - Если задача не найдена, верни 404

4. **PUT /todos/:id** — обновить задачу
   - Тело запроса: `{ "text": "Обновлённый текст", "completed": true }`
   - Если задача не найдена, верни 404

5. **DELETE /todos/:id** — удалить задачу
   - Если задача не найдена, верни 404

**Подсказки:**
- Храни массив задач в переменной на уровне модуля
- Используй `let nextId = 1` для генерации уникальных ID
- Используй `Array.find()`, `Array.findIndex()` для поиска
- Используй `req.body` для получения данных из POST/PUT запросов

---

## Задача 3: Добавь валидацию данных (Средний уровень)

**Цель:** Научиться проверять входящие данные.

**Задание:**
Добавь валидацию для маршрутов задач:

1. При создании задачи (`POST /todos`):
   - Проверь, что `text` существует и это строка
   - Проверь, что `text` не пустой и не состоит только из пробелов
   - Если валидация не прошла, верни статус 400 с сообщением об ошибке:
     ```json
     {
       "error": "Validation Error",
       "message": "Поле 'text' обязательно и должно быть непустой строкой"
     }
     ```

2. При обновлении задачи (`PUT /todos/:id`):
   - Проверь, что хотя бы одно поле (`text` или `completed`) присутствует
   - Если `text` передан, проверь, что это непустая строка
   - Если `completed` передан, проверь, что это булево значение

**Подсказки:**
- Создай функцию `validateTodo(data)` для валидации
- Используй `typeof` для проверки типов
- Используй `String.trim()` для проверки пустых строк
- Возвращай ошибку через `res.status(400).json()`

---

## Задача 4: Создай middleware для логирования (Средний уровень)

**Цель:** Научиться создавать собственные middleware.

**Задание:**
Создай отдельный файл `src/middleware/request-logger.js` с middleware для логирования запросов:

1. Middleware должен логировать в консоль:
   - Метод запроса
   - URL
   - Время обработки запроса (в миллисекундах)
   - Статус код ответа

2. Пример вывода в консоль:
   ```
   GET /todos 200 15ms
   POST /todos 201 8ms
   GET /todos/999 404 2ms
   ```

3. Подключи этот middleware в `server.js` так, чтобы он работал для всех маршрутов.

**Подсказки:**
- Используй `Date.now()` или `process.hrtime()` для измерения времени
- Используй событие `res.on('finish', ...)` для логирования после отправки ответа
- Middleware должен вызывать `next()` для передачи управления следующему обработчику

---

## Задача 5: Добавь обработку ошибок с разными статус-кодами (Продвинутый уровень)

**Цель:** Научиться создавать кастомные ошибки и обрабатывать их централизованно.

**Задание:**
1. Создай классы ошибок в файле `src/utils/errors.js`:
   ```javascript
   class NotFoundError extends Error {
     constructor(message) {
       super(message);
       this.statusCode = 404;
       this.name = 'NotFoundError';
     }
   }
   
   class ValidationError extends Error {
     constructor(message) {
       super(message);
       this.statusCode = 400;
       this.name = 'ValidationError';
     }
   }
   ```

2. Используй эти ошибки в маршрутах:
   - В `GET /todos/:id` выбрасывай `NotFoundError`, если задача не найдена
   - В валидации выбрасывай `ValidationError` при ошибках валидации

3. Обнови error handler в `server.js`, чтобы он правильно обрабатывал эти ошибки:
   - Если у ошибки есть `statusCode`, используй его
   - Иначе используй 500
   - Возвращай JSON с полями `error` и `message`

**Подсказки:**
- Используй `throw new NotFoundError(...)` для выбрасывания ошибок
- Express автоматически передаст ошибку в error handler middleware
- Error handler должен быть последним middleware (после всех маршрутов)

---

## Задача 6: Создай роутер для задач (Продвинутый уровень)

**Цель:** Научиться организовывать код с помощью Express Router.

**Задание:**
1. Создай файл `src/routes/todos.js` с роутером для всех маршрутов задач:
   ```javascript
   const express = require('express');
   const router = express.Router();
   
   // Все маршруты для /todos
   router.get('/', ...);
   router.post('/', ...);
   router.get('/:id', ...);
   // и т.д.
   
   module.exports = router;
   ```

2. В `server.js` подключи этот роутер:
   ```javascript
   const todosRouter = require('./routes/todos');
   app.use('/todos', todosRouter);
   ```

3. Создай отдельный файл `src/routes/math.js` для математических операций:
   - `GET /math/add?a=5&b=3` → `{ "result": 8 }`
   - `GET /math/multiply?a=4&b=7` → `{ "result": 28 }`
   - Используй функции из `src/modules/math.js`
   - Параметры получай через `req.query`

4. Подключи этот роутер в `server.js` как `app.use('/math', mathRouter)`

**Подсказки:**
- `express.Router()` создаёт мини-приложение для группы маршрутов
- `req.query` содержит query параметры (после `?` в URL)
- Используй `Number()` для преобразования строк в числа

---

## 🎓 Критерии оценки

- **Задача 1-2:** Базовое понимание Express маршрутов
- **Задача 3:** Понимание валидации данных
- **Задача 4:** Понимание middleware
- **Задача 5:** Понимание обработки ошибок
- **Задача 6:** Понимание организации кода и роутеров

## 💡 Дополнительные идеи (опционально)

- Добавь фильтрацию задач: `GET /todos?completed=true`
- Добавь пагинацию: `GET /todos?page=1&limit=10`
- Сохраняй задачи в файл JSON (используй `fs` модуль)
- Добавь CORS middleware для работы с фронтендом
- Создай API документацию в формате OpenAPI/Swagger

---

**Удачи! 🚀**

