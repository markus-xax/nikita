# Руководство по Prisma и Redis

Это руководство объясняет, когда и для чего используются PostgreSQL (через Prisma), Redis и как они работают вместе.

## 🗄️ PostgreSQL + Prisma ORM

### Что такое Prisma?

**Prisma** — это современный ORM (Object-Relational Mapping) для Node.js, который упрощает работу с базой данных.

### Зачем нужен Prisma?

1. **Типобезопасность** — автоматическая генерация типов на основе схемы
2. **Удобный API** — простой и интуитивный синтаксис вместо SQL
3. **Миграции** — версионирование схемы базы данных
4. **Валидация** — автоматическая проверка данных
5. **Производительность** — оптимизированные запросы

### Когда использовать PostgreSQL?

PostgreSQL используется для **постоянного хранения данных**:
- ✅ Пользователи и их профили
- ✅ Задачи, заметки, контент
- ✅ Транзакции и финансовые данные
- ✅ Любые данные, которые должны сохраняться навсегда

### Примеры использования Prisma:

```javascript
// Создание записи
const todo = await prisma.todo.create({
  data: {
    text: "Новая задача",
    completed: false
  }
});

// Получение всех записей
const todos = await prisma.todo.findMany();

// Получение по ID
const todo = await prisma.todo.findUnique({
  where: { id: 1 }
});

// Обновление
const updated = await prisma.todo.update({
  where: { id: 1 },
  data: { completed: true }
});

// Удаление
await prisma.todo.delete({
  where: { id: 1 }
});
```

---

## ⚡ Redis

### Что такое Redis?

**Redis** (Remote Dictionary Server) — это in-memory хранилище данных (key-value store), которое работает очень быстро, так как данные хранятся в оперативной памяти.

### Зачем нужен Redis?

1. **Кеширование** — хранение часто запрашиваемых данных
2. **Сессии** — хранение сессий пользователей
3. **Очереди** — управление задачами в фоне
4. **Счетчики** — подсчет лайков, просмотров
5. **Rate limiting** — ограничение количества запросов

### Когда использовать Redis?

Redis используется для **временного хранения данных**, которые:
- ✅ Часто запрашиваются
- ✅ Могут быть пересозданы из основной БД
- ✅ Нужны очень быстро
- ✅ Имеют ограниченное время жизни

### ⚠️ Важно помнить:

- Redis хранит данные **в памяти** — при перезапуске данные теряются (если не настроена персистентность)
- Redis **не заменяет** PostgreSQL — это дополнение для ускорения
- Данные в Redis должны быть **кешируемыми** — можно пересоздать из основной БД

---

## 🔄 Как они работают вместе?

### Схема работы с кешированием:

```
┌─────────────┐
│   Клиент    │
└──────┬──────┘
       │
       │ 1. Запрос данных
       ▼
┌─────────────────┐
│   Express API   │
└──────┬──────────┘
       │
       │ 2. Проверка кеша
       ▼
┌─────────────┐      Нет в кеше?     ┌──────────────┐
│    Redis    │ ◄─────────────────── │   Prisma     │
│  (Кеш)      │                      │ (PostgreSQL) │
└──────┬──────┘                      └──────┬───────┘
       │                                     │
       │ Есть в кеше?                        │ 3. Запрос к БД
       │                                     │
       │ 4. Возврат из кеша                 │ 4. Сохранение в кеш
       │                                     │
       └──────────────┬──────────────────────┘
                      │
                      ▼
              ┌──────────────┐
              │   Клиент     │
              └──────────────┘
```

### Пример из нашего кода:

```javascript
// 1. Пытаемся получить из Redis (быстро!)
const cachedTodos = await getCache('todos:all');

if (cachedTodos) {
  return res.json(cachedTodos); // ✅ Вернули из кеша за миллисекунды
}

// 2. Если нет в кеше, идем в PostgreSQL (медленнее)
const todos = await prisma.todo.findMany();

// 3. Сохраняем в Redis для следующих запросов
await setCache('todos:all', todos, 300); // TTL = 5 минут

return res.json(todos);
```

---

## 📊 Сравнение производительности

| Операция | PostgreSQL | Redis |
|----------|-----------|-------|
| Чтение | ~1-10ms | ~0.1ms |
| Запись | ~1-10ms | ~0.1ms |
| Хранение | Постоянное | Временное (в памяти) |
| Объем данных | Огромный (ТБ) | Ограничен (ГБ) |

---

## 🎯 Практические примеры использования

### 1. Кеширование списка задач

**Проблема:** Список задач запрашивается очень часто, но редко меняется.

**Решение:** Кешируем в Redis на 5 минут.

```javascript
// Первый запрос → идет в PostgreSQL
// Второй запрос (в течение 5 мин) → идет из Redis (в 10-100 раз быстрее!)
```

### 2. Кеширование отдельной задачи

**Проблема:** Пользователи часто открывают одну и ту же задачу.

**Решение:** Кешируем каждую задачу на 10 минут.

### 3. Инвалидация кеша

**Проблема:** Когда задача обновляется, кеш становится устаревшим.

**Решение:** При изменении данных удаляем соответствующий кеш:

```javascript
// Обновили задачу
await prisma.todo.update({ ... });

// Удаляем кеш этой задачи и списка
await deleteCache('todos:123');
await deleteCache('todos:all');
```

---

## 🚀 Настройка и запуск

### 1. Установка зависимостей

```bash
cd backend
npm install
```

### 2. Настройка PostgreSQL

Создайте базу данных:

```bash
createdb nikita_db
```

Или через psql:

```sql
CREATE DATABASE nikita_db;
```

### 3. Настройка переменных окружения

Создайте файл `.env` в папке `backend/`:

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nikita_db?schema=public"
REDIS_HOST=localhost
REDIS_PORT=6379
```

### 4. Запуск миграций Prisma

```bash
npm run db:migrate
```

Это создаст таблицы в PostgreSQL на основе схемы `prisma/schema.prisma`.

### 5. Генерация Prisma Client

```bash
npm run db:generate
```

### 6. Запуск Redis

```bash
# macOS (через Homebrew)
brew services start redis

# Linux
sudo systemctl start redis

# Docker
docker run -d -p 6379:6379 redis
```

### 7. Запуск сервера

```bash
npm start
```

---

## 📝 Полезные команды

```bash
# Prisma Studio - визуальный редактор БД
npm run db:studio

# Создать новую миграцию
npm run db:migrate

# Сгенерировать Prisma Client
npm run db:generate
```

---

## 🎓 Ключевые выводы

1. **PostgreSQL (Prisma)** = постоянное хранилище данных
2. **Redis** = временное хранилище для ускорения
3. **Используйте вместе** для максимальной производительности
4. **Кешируйте** часто запрашиваемые данные
5. **Инвалидируйте кеш** при изменении данных

---

## 📚 Дополнительные ресурсы

- [Документация Prisma](https://www.prisma.io/docs)
- [Документация Redis](https://redis.io/docs)
- [Prisma vs SQL](https://www.prisma.io/docs/concepts/overview/prisma-in-your-stack/is-prisma-an-orm)
