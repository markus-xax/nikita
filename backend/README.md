
---

## 🚀 Быстрый старт
1. Установи Node.js 18+ (LTS). Проверь версию:
   ```bash
   node -v
   npm -v
   ```
2. В этой папке уже есть минимальный `package.json`. Установи зависимости (их пока нет, но команда создаст `package-lock.json`):
   ```bash
   cd backend
   npm install
   ```
3. Запусти учебный сервер:
   ```bash
   npm run start
   ```
   В консоли появится лог, а в браузере или Postman можно открыть `http://localhost:3000/`.

---

## 🧭 Дорожная карта уроков
Каждый блок стоит изучать последовательно: прочитать теорию, выполнить упражнение, придумать небольшие вариации.

### 1. Node-runtime и модули
- Разобраться с CommonJS (`require`) и ES Modules (`import`). В примерах используется CommonJS, так как он доступен «из коробки».
- Файл `src/modules/math.js` демонстрирует экспорт/импорт функций.
- Упражнение: добавь новую функцию в модуль, используй её в `server.js`.

### 2. Работа с файлами
- Посмотри на `src/utils/file-logger.js`. Здесь используется встроенный модуль `fs`, записывающий запросы в `logs/requests.log`.
- Упражнение: добавь запись ошибки или время ответа.

### 3. HTTP без фреймворков
- `src/server.js` — базовый HTTP-сервер на `http.createServer`. Нет ни Express, ни других библиотек. Это поможет понять, что делает любой веб-фреймворк «под капотом».
- В сервере реализованы маршруты:
  - `GET /` — приветствие.
  - `GET /status` — здравствуй, JSON.
  - `POST /echo` — возвращает тело запроса (важно обработать поток данных).
- Упражнение: добавь свой маршрут, например `GET /time`, который возвращает текущее время.

### 4. Работа с окружением
- В `src/config.js` читается порт из `.env` (если создашь файл) или используется значение по умолчанию.
- Упражнение: сделай конфиг для режима разработки/продакшена (debug‑логи, префикс API и т.д.).

### 5. Структура проекта
```
backend/
├── README.md
├── package.json
├── src/
│   ├── config.js
│   ├── modules/
│   │   └── math.js
│   ├── server.js
│   └── utils/
│       └── file-logger.js
└── logs/
    └── requests.log        # создаётся автоматически при запросах
```
- **logs/**: папка будет создана автоматически при первом запросе. Хранит простейшие логи.
- **src/modules**: отдельные файлы с логикой, чтобы не писать всё в `server.js`.
- **src/utils**: утилиты, которые можно переиспользовать (логгеры, преобразования и т.д.).

---

## 🔍 Задачи для самостоятельной практики
1. **Добавь обработку 404**: если маршрут не найден, возвращай статус 404 и JSON с сообщением.
2. **Разбор заголовков**: в `POST /echo` логируй заголовки запроса.
3. **Body-parser**: создай новую утилиту, которая аккуратно собирает тело запроса (JSON, текст, формы).
4. **Mock база данных**: сделай in-memory список задач (`todo`), реализуй CRUD через новые маршруты.
5. **Express (опционально)**: после понимания «голого» HTTP попробуй переписать сервер на Express, отмечая, что стало проще.

---

## 📚 Дополнительные ресурсы
- [Node.js Docs](https://nodejs.org/en/docs)
- [The Node.js Handbook](https://www.freecodecamp.org/news/the-node-js-handbook/)
- [HTTP в Node.js без фреймворков](https://nodejs.dev/en/learn)
- [Express Guide](https://expressjs.com/) — следующий шаг после освоения базовых вещей.

---

Удачи в изучении! Совмещай чтение с практикой: запускай сервер, меняй код, смотри, как ведут себя запросы. После освоения этих блоков переход к базе данных или интеграции с фронтендом станет гораздо легче.

